## ref: https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: nginx-service-internal
  namespace: service
  labels:
    app: nginx
    type: service
spec:
  type: ClusterIP
  selector:
    app: nginx
    type: service
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
